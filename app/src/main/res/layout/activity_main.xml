<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <TextView
            android:id="@+id/locationText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Locating…"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="@color/textPrimary"
            android:gravity="center"
            android:layout_marginTop="16dp" />

        <TextView
            android:id="@+id/dateText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="@color/textSecondary"
            android:gravity="center"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="24dp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/card_background"
            android:padding="4dp">

            <!-- Fajr -->
            <LinearLayout style="@style/PrayerRow">
                <TextView style="@style/PrayerLabel" android:text="Fajr" />
                <TextView android:id="@+id/fajrTime" style="@style/PrayerValue" />
            </LinearLayout>
            <View style="@style/RowDivider" />

            <!-- Sunrise -->
            <LinearLayout style="@style/PrayerRow">
                <TextView style="@style/PrayerLabel" android:text="Sunrise" />
                <TextView android:id="@+id/sunriseTime" style="@style/PrayerValue" />
            </LinearLayout>
            <View style="@style/RowDivider" />

            <!-- Dhuhr -->
            <LinearLayout style="@style/PrayerRow">
                <TextView style="@style/PrayerLabel" android:text="Dhuhr" />
                <TextView android:id="@+id/dhuhrTime" style="@style/PrayerValue" />
            </LinearLayout>
            <View style="@style/RowDivider" />

            <!-- Asr -->
            <LinearLayout style="@style/PrayerRow">
                <TextView style="@style/PrayerLabel" android:text="Asr" />
                <TextView android:id="@+id/asrTime" style="@style/PrayerValue" />
            </LinearLayout>
            <View style="@style/RowDivider" />

            <!-- Maghrib -->
            <LinearLayout style="@style/PrayerRow">
                <TextView style="@style/PrayerLabel" android:text="Maghrib" />
                <TextView android:id="@+id/maghribTime" style="@style/PrayerValue" />
            </LinearLayout>
            <View style="@style/RowDivider" />

            <!-- Isha -->
            <LinearLayout style="@style/PrayerRow">
                <TextView style="@style/PrayerLabel" android:text="Isha" />
                <TextView android:id="@+id/ishaTime" style="@style/PrayerValue" />
            </LinearLayout>

        </LinearLayout>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/refreshButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="Refresh Location" />

    </LinearLayout>
</ScrollView>
